
@foreach (var product in Products)
{
    <div class="card w-25">
        <div class="card-body">
            <h5 class="card-title">@product.Name</h5>            
            <blockquote class="card-text">$@product.Price</blockquote>
            @if (DisplayBuyButton)
            {                
                <span>
                Quantity:
                <input @bind="Quantity" />
                <button class="btn btn-primary" type="button" @onclick="() => AddToCart(product, Quantity)">Add</button>
                </span>
            }
            else
            {
                <blockquote class="card-text"> Quantity @product.Quantity</blockquote>
            }
        </div>
    </div>
}